# Custom partition table for ESP32-S3-CAM N16R8 (16MB flash)
# ============================================================
# Layout:
#   nvs       — non-volatile storage (WiFi creds, calibration, user config)
#   otadata   — OTA boot selector
#   app0      — application slot A (active)
#   app1      — application slot B (OTA target)
#   spiffs    — file system for web assets, audio clips, ML models
#   coredump  — panic dump area for post-mortem

# Name,    Type,  SubType,  Offset,   Size,    Flags
nvs,       data,  nvs,      0x9000,   0x6000,
otadata,   data,  ota,      0xf000,   0x2000,
app0,      app,   ota_0,    0x10000,  0x300000,
app1,      app,   ota_1,    0x310000, 0x300000,
spiffs,    data,  spiffs,   0x610000, 0x9E0000,
coredump,  data,  coredump, 0xFF0000, 0x10000,
